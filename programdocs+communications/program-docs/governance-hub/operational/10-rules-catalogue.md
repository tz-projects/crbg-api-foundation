# Governance Rules Catalogue

> The readable companion to the Spectral ruleset (not the ruleset file itself). Teams cannot remediate against rules they can't interpret.

## How to read this catalogue
Explain the fields used per rule.

## Rule entry template
- **Rule ID** —
- **What it checks** —
- **Why it matters** —
- **Severity** (error / warning) —
- **Violating example** —
- **Conformant fix** —

## Built-in OAS rules
One entry per built-in rule, using the template.

## Custom rules — naming conventions
Naming-standard rules (planned).

## Custom rules — versioning standards
Versioning rules (planned).

## Severities & what they mean
How error vs warning maps to gate behaviour.
