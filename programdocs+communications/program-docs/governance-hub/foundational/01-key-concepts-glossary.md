# Key Concepts & Glossary

> Shared vocabulary. Every other page is ambiguous until these terms mean exactly one thing. Keep definitions one line each.

## Core concepts (short narrative)
A few sentences tying the pieces together: a spec is evaluated against a ruleset, producing a conformance result; a conformant spec gets published; the pipeline checks publish state.

## Glossary
One line per term:
- **API spec / OAS** —
- **Ruleset** —
- **Rule** / **rule ID** / **rule severity (error/warning)** —
- **Conformant** —
- **Published** —
- **Report-only mode** —
- **Gate (soft / hard)** —
- **Grace period** —
- **Waiver / exception** —
- **Source of truth** —
- **Designer vs Consumer (license types)** —
- **New API vs existing API** —
