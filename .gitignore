# --- OS / editor ---
.DS_Store
Thumbs.db
*.swp
*.swo

# --- Secrets / local env ---
.env
.env.*
!.env.example
.envrc.local

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.python-version-local
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pytype/
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover

# --- Node / TypeScript ---
node_modules/
.pnpm-store/
*.tsbuildinfo
.next/
.cache/
.turbo/
.vitest-cache/
coverage/

# --- Build outputs ---
out/
build/

# --- Scanner outputs (local artifacts, not source) ---
projects/*/output/
projects/*/reports/
projects/*/*.local.*

# --- Report generator Tier 2/3 inputs (may contain internal team data / CIO ask wording) ---
# Track these in a private location, or `git add -f` if your copy is safe to commit.
# `*.example.*` files alongside them stay tracked.
projects/reports/ownership.yaml
projects/reports/ownership.yml
projects/reports/rule_display_names.yaml
projects/reports/rule_display_names.yml
projects/reports/cop_guidance.yaml
projects/reports/cop_guidance.yml
projects/reports/asks.md

# --- Direnv ---
.direnv/

# --- Claude Code (per-machine local settings) ---
.claude/settings.local.json

# --- Captured HTTP transcripts (may contain session cookies / CSRF tokens) ---
http-requests.md
*.har
*.har.sanitized

# --- Diagnostic outputs ---
server-cert.pem

# --- Lambda build outputs ---
*-lambda.zip
scanner-source.zip
scanner-deps-layer.zip
scanner-code.zip
scanner-project.zip
