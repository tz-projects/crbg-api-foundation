# direnv: auto-activate uv-managed venv when entering this directory.
# Run `direnv allow` once after cloning to authorize.
if [ -f .venv/bin/activate ]; then
  source .venv/bin/activate
fi
dotenv_if_exists ../.env
