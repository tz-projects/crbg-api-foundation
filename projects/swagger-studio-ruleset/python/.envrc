if [ -f .venv/bin/activate ]; then
  source .venv/bin/activate
fi
# Shared .env lives in the scanner sub-project — both publishers + scanner
# read the same credentials from one place.
dotenv_if_exists ../../swagger-studio-scanner/.env
